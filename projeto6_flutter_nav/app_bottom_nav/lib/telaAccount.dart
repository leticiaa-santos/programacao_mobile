import 'package:flutter/material.dart';

class TelaAccount extends StatelessWidget {
  const TelaAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}